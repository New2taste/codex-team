interface:
  display_name: "AI Workflow Orchestration"
  short_description: "Route bounded work with evidence gates"
  default_prompt: "Use $ai-workflow:orchestration to run a fail-closed preflight."
